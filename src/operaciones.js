export const suma = (num1, num2) => {
    return num1 + num2;
}

const resta = (num1, num2) => {
    return num1 - num2;
}

const multiplicacion = (num1, num2) => {
    return num1 * num2;
}

export const saludo = 'Hola, soy Daniela Domínguez Peña, saludos desde un módulo';